%% prep
A5P1;
Correlations = corrcoef(Mcldt, "Rows","pairwise");
stds = std(Mcldt,"omitmissing");

%% plotting
tiledlayout("horizontal")
nexttile;
heatmap(N, N, Correlations, "Colormap",jet)
nexttile;
y_loc = 1:20;
y_loc = y_loc - 0.5;
plot(stds, y_loc)
set(gca, "YAxisLocation", "right")
set(gca, )
set(gca, "YTickLabel",N)